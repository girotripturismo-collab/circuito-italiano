import {
  initHeader,
  initReveal,
  initSmoothScroll,
  initStrips
} from './index.js';

document.addEventListener('DOMContentLoaded', () => {
  initHeader();
  initReveal();
  initStrips();
  initSmoothScroll();
});
